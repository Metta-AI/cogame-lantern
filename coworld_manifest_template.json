{
  "$schema": "https://raw.githubusercontent.com/Metta-AI/coworld/main/src/coworld/coworld_manifest_schema.json",
  "episode_timeout_minutes": 20,
  "tags": [
    "hide-and-seek",
    "asymmetric-teams",
    "construction",
    "physics",
    "real-time",
    "fog-of-war",
    "llm-driven",
    "six-player",
    "zero-sum",
    "tool-use"
  ],
  "game": {
    "name": "lantern",
    "replay_viewer": {
      "bundle": "static-replay-viewer"
    },
    "description": "Lantern: 3v3 hide-and-seek in the dark on a warehouse floor, played twice with the sides swapped. Six cogs share a walled 1235x659 px floor lit only by the seekers' flashlights. In each half one trio - the hiders - gets a 30-second lights-on build act to shove and bolt 48x48 wooden crates into a fort; then the lights go out, the seekers' pen door opens, and the seekers get 75 seconds to sweep the dark. A seeker's lit set is a 420 px, 50-degree beam plus a 60 px bubble, occluded by walls and by every crate that is still standing, and the three seekers share one radio. Held in a beam for half a second, or touched, and a hider is found. A locked crate cannot be shoved by anyone - only a 3-second, very loud pry breaks it - so the fort is a real asset and breaching it is a real decision. A hider scores one tick per tick it is not yet found; then the sides swap, the map resets to its exact starting layout, and the halves are compared: score(Moth) = 0.5 + 0.5 * (f(Moth) - f(Owl)), exactly zero-sum between the sides. The game is LLM-driven: every five seconds the server sends each seat's policy prompt plus that seat's view (its role, its clock, what it can actually see, the sounds it can hear and - for a seeker - its five-band proximity heartbeat) to Claude as ONE PARALLEL BATCH, and a deterministic control layer executes the returned order at 24 Hz. A POLICY IS JUST A PROMPT - build one by reusing the published player runnable and setting the PLAYER_PROMPT environment variable to your strategy. Two scripted baselines (warden and moth) play any seat that registers as scripted, and every seat when no LLM credentials are available, so episodes always complete. In-game a cog is only ever Moth-1..Owl-3; real player names appear spectator-side only.",
    "owner": "daveey@softmax.com",
    "runnable": {
      "type": "game",
      "image": "{{GAME_IMAGE}}",
      "run": [
        "/bin/lantern"
      ],
      "env": {
        "ANTHROPIC_API_KEY_URI": "secret://coworld/lantern/anthropic_api_key"
      },
      "source_url": "https://github.com/Metta-AI/cogame-lantern/tree/main"
    },
    "config_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tokens",
        "players"
      ],
      "properties": {
        "tokens": {
          "description": "One connection token per player slot, indexed by slot.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "players": {
          "description": "One player display-name object per seat, indexed by slot.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        "slots": {
          "description": "Optional team pin, indexed by slot. Seat to team is fixed in lantern (even slots are Moth, odd slots are Owl); a pin that disagrees is refused rather than reinterpreted.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "team": {
                "type": "string",
                "enum": [
                  "moth",
                  "owl"
                ]
              }
            }
          }
        },
        "num_agents": {
          "description": "Seats in the episode. Lantern is 2 teams x 3 cogs.",
          "type": "integer",
          "minimum": 6,
          "maximum": 6,
          "default": 6
        },
        "seed": {
          "description": "Episode seed. Unpinned it is randomised at startup, before the config overlay, so every seed-derived draw follows the final seed.",
          "type": "integer",
          "minimum": 0
        },
        "prepTicks": {
          "description": "Ticks in each half's lights-on build act (24 ticks = 1 s). Must be a whole multiple of turnTicks.",
          "type": "integer",
          "minimum": 120,
          "maximum": 2880,
          "default": 720
        },
        "huntTicks": {
          "description": "Ticks in each half's hunt act. Must be a whole multiple of turnTicks.",
          "type": "integer",
          "minimum": 240,
          "maximum": 7200,
          "default": 1800
        },
        "turnTicks": {
          "description": "Ticks between decision turns. 120 = one order every 5 s.",
          "type": "integer",
          "minimum": 24,
          "maximum": 600,
          "default": 120
        },
        "halves": {
          "description": "Halves played. Lantern always plays exactly two, so the two sides are compared on identical geometry.",
          "type": "integer",
          "minimum": 2,
          "maximum": 2,
          "default": 2
        },
        "turnBudgetSeconds": {
          "description": "Outer wall-clock budget for one decision turn, covering both LLM attempts.",
          "type": "number",
          "minimum": 1,
          "maximum": 60,
          "default": 13
        },
        "wallClockBudgetSeconds": {
          "description": "Hard engine stop. Must stay inside 60 percent of the platform's episode timeout; the budget guard settles the match on the scripted layer well before this fires.",
          "type": "number",
          "minimum": 30,
          "maximum": 700,
          "default": 660
        },
        "playerConnectTimeoutSeconds": {
          "description": "Bounded wait for the seats to connect before the match starts anyway.",
          "type": "number",
          "minimum": 5,
          "maximum": 300,
          "default": 90
        },
        "lanternRangePx": {
          "description": "Flashlight range in map pixels.",
          "type": "integer",
          "minimum": 60,
          "maximum": 1200,
          "default": 420
        },
        "lanternConeBrads": {
          "description": "Half-angle of the beam in brads (256 per turn). 18 is a 50.6-degree beam.",
          "type": "integer",
          "minimum": 2,
          "maximum": 128,
          "default": 18
        },
        "visionBubblePx": {
          "description": "Omnidirectional sight bubble around a seeker.",
          "type": "integer",
          "minimum": 0,
          "maximum": 400,
          "default": 60
        },
        "crateCount": {
          "description": "Crates placed from the map file, in order.",
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "default": 10
        },
        "lockTicks": {
          "description": "Ticks a hider must hold still to bolt a crate down.",
          "type": "integer",
          "minimum": 1,
          "maximum": 240,
          "default": 24
        },
        "pryTicks": {
          "description": "Ticks a seeker must hold still to breach a locked crate.",
          "type": "integer",
          "minimum": 1,
          "maximum": 480,
          "default": 72
        },
        "lockOnTicks": {
          "description": "Consecutive lit ticks that turn a spot into a find.",
          "type": "integer",
          "minimum": 1,
          "maximum": 240,
          "default": 12
        },
        "maxLocksPerHider": {
          "description": "Locks each hider may spend per half.",
          "type": "integer",
          "minimum": 0,
          "maximum": 10,
          "default": 3
        },
        "mapPath": {
          "description": "The authored map to load from data/. Lantern ships one: vault.",
          "type": "string",
          "default": "vault"
        },
        "showPlayerLabels": {
          "description": "Spectator-side only: show real player names on the scorebug plates.",
          "type": "boolean",
          "default": true
        },
        "gameOverTicks": {
          "description": "Ticks the viewer holds on the endcard after the final tick.",
          "type": "integer",
          "minimum": 0,
          "maximum": 600,
          "default": 96
        },
        "episodeTimeoutSeconds": {
          "description": "Assumed platform kill time when the env is silent. Lantern plays inside 60 percent of it.",
          "type": "number",
          "minimum": 60,
          "maximum": 3600,
          "default": 1200
        },
        "model": {
          "description": "Anthropic model id for the direct API transport (the hosted Bedrock sidecar picks its own).",
          "type": "string",
          "default": "claude-sonnet-5"
        },
        "maxOutputTokens": {
          "description": "max_tokens for one order. 400 truncates the JSON; 900 does not.",
          "type": "integer",
          "minimum": 200,
          "maximum": 4000,
          "default": 900
        }
      }
    },
    "results_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "aliases",
        "crates_broken",
        "crates_locked",
        "crates_pushed",
        "end_rule",
        "fallback_causes",
        "fallback_turns",
        "final_tick",
        "final_turn",
        "finds",
        "halves_played",
        "hid_in_half",
        "hidden_seconds",
        "hidden_ticks",
        "hunt_ticks_played",
        "llm_turns",
        "names",
        "policy_kinds",
        "reason",
        "scores",
        "seed",
        "team_hidden_frac",
        "team_hidden_seconds",
        "teams",
        "win",
        "winner"
      ],
      "properties": {
        "names": {
          "description": "Real policy display names, indexed by slot. Spectator-side only: in-game a cog is Moth-1..Owl-3.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "string"
          }
        },
        "aliases": {
          "description": "The in-game alias of each seat.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "string"
          }
        },
        "teams": {
          "description": "Moth or Owl, by slot. Even slots are Moth, odd are Owl.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "string",
            "enum": [
              "Moth",
              "Owl"
            ]
          }
        },
        "hid_in_half": {
          "description": "Which half this seat spent hiding (1 for Moth, 2 for Owl).",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "policy_kinds": {
          "description": "llm or scripted, by slot.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "string",
            "enum": [
              "llm",
              "scripted"
            ]
          }
        },
        "scores": {
          "description": "The seat's team score in [0, 1]. Higher is better; the two team scores sum to 1.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "number"
          }
        },
        "win": {
          "description": "True when this seat's team scored above 0.5. All false on a draw.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "boolean"
          }
        },
        "hidden_ticks": {
          "description": "Hunt ticks in this seat's hiding half during which it was unfound.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "hidden_seconds": {
          "description": "The same, in seconds.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "number"
          }
        },
        "finds": {
          "description": "Hiders this seat found while seeking.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "crates_pushed": {
          "description": "Crate shoves this seat contributed, counted at most once per crate per 12 ticks.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "crates_locked": {
          "description": "Crates this seat bolted down.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "crates_broken": {
          "description": "Locked crates this seat pried open.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "team_hidden_frac": {
          "description": "f(team) in [0, 1], index 0 = Moth, 1 = Owl.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number"
          }
        },
        "team_hidden_seconds": {
          "description": "Total seconds hidden per team, index 0 = Moth, 1 = Owl.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number"
          }
        },
        "reason": {
          "description": "Why the episode ended.",
          "type": "string",
          "enum": [
            "complete",
            "deadline",
            "fault"
          ]
        },
        "end_rule": {
          "description": "The detail behind `reason`.",
          "type": "string",
          "enum": [
            "full_time",
            "wall_clock",
            "sim_fault",
            "host_error"
          ]
        },
        "winner": {
          "description": "0 Moth, 1 Owl, null on a draw.",
          "type": [
            "integer",
            "null"
          ]
        },
        "final_tick": {
          "description": "Ticks simulated.",
          "type": "integer"
        },
        "final_turn": {
          "description": "Decision turns played.",
          "type": "integer"
        },
        "halves_played": {
          "description": "Halves whose hunt act ran at all.",
          "type": "integer"
        },
        "hunt_ticks_played": {
          "description": "Hunt ticks actually simulated per half - the score denominator.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer"
          }
        },
        "seed": {
          "description": "The episode seed the replay re-derives from.",
          "type": "integer"
        },
        "llm_turns": {
          "description": "Turns this seat's order came from the LLM.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "fallback_turns": {
          "description": "Turns this seat fell back to the scripted order.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "integer"
          }
        },
        "fallback_causes": {
          "description": "Per-seat fallback tally by cause.",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "timeout",
              "parse_error",
              "transport_error",
              "no_credentials",
              "budget_guard"
            ],
            "properties": {
              "timeout": {
                "type": "integer"
              },
              "parse_error": {
                "type": "integer"
              },
              "transport_error": {
                "type": "integer"
              },
              "no_credentials": {
                "type": "integer"
              },
              "budget_guard": {
                "type": "integer"
              }
            }
          }
        }
      }
    },
    "protocols": {
      "player": {
        "type": "text",
        "value": "lantern.player.v1 - JSON text frames over the websocket named by COWORLD_PLAYER_WS_URL (already carrying ?slot=N&token=T; a bad token 403s and a duplicate connection 409s). A lantern policy is a prompt and every decision is made in the GAME server, so the player container has exactly one job: on connect it sends ONE frame, {\"type\":\"register\",\"prompt\":\"<strategy text or empty>\",\"scripted\":\"warden\"|\"moth\"|null,\"policy\":\"<free label, <=48 runes>\"}, and thereafter only receives. PLAYER_SCRIPTED=warden|1|true|yes selects the warden baseline, moth the weaker moth baseline; a seat that registers with neither field, or never registers at all, plays warden and the match still goes to full time. An over-long prompt is truncated at 4000 runes, never rejected. The server replies {\"type\":\"welcome\",\"protocol\":\"lantern.player.v1\",\"slot\":N,\"alias\":\"Moth-1\",\"team\":\"Moth\",\"hides_in_half\":1,\"turns\":42}, then one informational frame per decision turn, {\"type\":\"turn\",\"turn\":T,\"tick\":K,\"half\":H,\"act\":\"build\"|\"hunt\",\"role\":\"hider\"|\"seeker\",\"view\":{...},\"order_source\":\"llm\"|\"scripted\"|\"fallback\"} - the seat is not required to answer it - and finally {\"done\":true,\"result\":{...the results document...}} before closing. The view is role-shaped: a hider sees the full static map, all ten crate positions and states, its own trio, seekers within 700 px with line of sight, the bearing and band of any beam within 220 px, and break-ring sounds; a seeker sees the static map, its own heartbeat band, only what the team's three lanterns light right now, its team-mates' bands, audible sound rings and the found list. Neither role ever sees the opponent's prompts, notes or say strings, the real player names behind the aliases, or the seed. The order the server asks Claude for is a single JSON object: {\"intent\":...,\"target\":[x,y],\"crate\":\"C0\"..\"C9\"|null,\"aim\":\"sweep|hold|track|target\",\"crawl\":bool,\"note\":\"<=140 runes\",\"say\":\"<=32 runes\"} - hider intents push/lock/hide/flee/scout/wait, seeker intents sweep/beeline/chase/pry/hold/wait. Parsing is tolerant (fences stripped, outermost balanced object taken, numeric strings and integer crate ids accepted) and every field is repaired rather than rejected; only an unrecoverable intent costs the one retry, and only a failed retry falls back to the warden order plus a fallback event. Every recorded string is truncated on RUNE boundaries, never bytes."
      },
      "global": {
        "type": "text",
        "value": "The spectator surface. GET /healthz answers {\"ok\":true}. WS /global streams a whole-world snapshot on connect and every few seconds after: {\"type\":\"state\",\"game\":\"lantern\",\"tick\",\"turn\",\"turns\",\"half\",\"act\",\"act_left_ticks\",\"map\":{\"w\",\"h\",\"walls\",\"nooks\",\"pen\"},\"cogs\":[{\"slot\",\"alias\",\"team\",\"role\",\"pos\",\"aim\",\"crawl\",\"found\",\"hidden_ticks\",\"locks_used\",\"heartbeat\",\"intent\",\"note\",\"say\",\"source\"}],\"crates\":[{\"id\",\"pos\",\"state\"}],\"sounds\":[{\"kind\",\"pos\",\"radius\",\"age_ticks\"}],\"policyNames\",\"colors\",\"started\",\"done\",\"connected\"}. Nothing is hidden from a spectator - that is the point of the second name space: the board labels cogs Moth-1..Owl-3 and the scorebug maps those back to real player names. mummy hands Ping frames to the application, so /global answers them with a Pong (the certifier pings to check the game is alive). HOSTED REPLAYS ARE NEVER A POD: the manifest declares \"replay_viewer\": {\"bundle\": \"static-replay-viewer\"}, and the bundle built by tools/build_replay_viewer.sh re-derives every frame in the browser from seed + map + controls_b64 with the same integer Nim sim the server ran, validating itself against the recorded keyframe digests and surfacing any divergence as data-replay-mismatch-tick. The bundle fetches nothing but the replay URL it was handed and posts {src:\"coworld-replay\",type:\"loading\"|\"ready\"|\"error\"} to its embedding page. The game server also serves /client/replay off the identical dist for local viewing, and /replay-data in replay mode. The replay bytes themselves are strict UTF-8 JSON with protocol \"lantern.replay.v1\": config, the map inlined verbatim, names/aliases/teams/policy_kinds/colors, the phase table, controls_b64 (tick_count x 6 x 4 bytes), a keyframe every 24 ticks with an FNV-1a state digest, the full event stream and the results document."
      }
    },
    "docs": {
      "readme": {
        "type": "text",
        "value": "# lantern\n\n**3v3 hide-and-seek in the dark, played twice with the sides swapped.**\n\nSix cogs share a walled 1235 × 659 px warehouse floor lit only by the seekers'\nflashlights. In each half one trio — the **hiders** — gets a 30-second\nlights-on build act to shove and bolt wooden crates into a fort. Then the\nlights go out, the seekers' pen door opens, and the seekers get 75 seconds to\nsweep the dark and find all three. A hider's score is the time it survives\nunfound. Then the sides swap, the map resets to its exact starting layout, and\nthe second half is played under identical conditions. **The team that hid\nlonger wins.**\n\nA **policy is just a prompt.** Every seat is an LLM policy: once every five\nseconds the game server hands that seat's prompt plus that seat's view of the\nworld to Claude and asks for ONE order — an intent, a target, a crate, an aim\nmode and a crawl flag. A deterministic control layer executes that order at\n24 Hz for the next five seconds. Field your own policy by reusing the\npublished image and setting `PLAYER_PROMPT`:\n\n```bash\ncoworld upload-policy coworld-lantern:latest --name my-lantern \\\n  --run /bin/lantern-player \\\n  --secret-env PLAYER_PROMPT=\"Build a warren, then vanish into it...\"\n```\n\nTwo scripted baselines ship in the same image and play any seat that sets\n`PLAYER_SCRIPTED=warden` or `PLAYER_SCRIPTED=moth` — and every seat when no\nLLM credentials are available at all, so an episode always completes.\n\n## The shape of a match\n\n| Segment | Ticks | Sim time | Decision turns |\n|---|---|---|---|\n| Half 1 — build (Moth hides) | 0 – 719 | 30 s | 6 |\n| Half 1 — hunt | 720 – 2519 | 75 s | 15 |\n| Half 2 — build (Owl hides) | 2520 – 3239 | 30 s | 6 |\n| Half 2 — hunt | 3240 – 5039 | 75 s | 15 |\n| **Total** | **5040** | **210 s** | **42** |\n\nSeats are fixed: **even slots (0, 2, 4) are team Moth, odd slots (1, 3, 5) are\nteam Owl**, so the two champions always land on opposite sides. In-game a cog\nis only ever `Moth-1`…`Owl-3`; the real player names appear spectator-side\nonly, in the scorebug, the endcard and the results.\n\n## Scoring\n\n```\nf(team)     = sum of the team's hidden ticks / (3 * hunt ticks in its hiding half)\nscore(Moth) = 0.5 + 0.5 * (f(Moth) - f(Owl))\nscore(Owl)  = 1 - score(Moth)\n```\n\nHigher is better, `score ∈ [0, 1]`, and the two scores always sum to 1: the\ngame is exactly zero-sum between the sides, which is what makes cross-episode\nwin-trading pointless. Hiding well and seeking well are the same skill on this\nscale — a team's own `f` is its hiding, and the opponent's `f` is what its\nseeking held down.\n\nFull rules: [docs/RULES.md](docs/RULES.md). Wire protocol:\n[docs/PROTOCOL.md](docs/PROTOCOL.md). Design note:\n[docs/plans/2026-08-22-lantern-design.md](docs/plans/2026-08-22-lantern-design.md).\n\n## Watching a match\n\nReplays are a **static wasm bundle**, never a pod. The bundle re-derives every\nframe in the browser from `seed` + `map` + `controls_b64` with the same integer\nNim sim the server ran, and checks itself against the recorded keyframe\ndigests. What you see:\n\n- flashlight cones sweeping a near-black floor, with crates throwing the\n  shadows the hiders are standing in;\n- unlit hiders as 30 %-alpha silhouettes — the dramatic irony the seekers do\n  not get (bound to the `spoilers` toggle, default on);\n- a proximity **heartbeat bar** per seeker, straight off the keyframe `hb`\n  array, so it is exactly what that seeker was told;\n- a spotlight **burst** on a find, with the banner and the feed line;\n- the 30-second build acts run as a **4× timelapse**, then 1× the moment the\n  hunt starts;\n- a **side-swap intermission card** at half time so the reset is visibly real.\n\n## Running it locally\n\n```bash\ndocker build -t coworld-lantern:latest .\ntools/ci/docker_smoke.sh coworld-lantern:latest    # one full episode, raw docker\ntools/build_replay_viewer.sh \"$PWD/dist/static-replay-viewer\"\n```\n\nTests are plain Nim programs, one file per concern:\n\n```bash\nnim r --path:src tests/test_rules.nim\n```\n\nCI runs every `tests/*.nim` twice, debug and `-d:release`.\n\n## Layout\n\n```\nsrc/lantern.nim          the game server entrypoint (seed randomisation lives here)\nsrc/lantern_player.nim   the thin player: register, then receive until done\nsrc/lantern/             types, arena, crates, sim, rules, control, orders,\n                         baselines, llm, state, config, roster, events, labels,\n                         broadcast, render, replay, server\nclient/                  the broadcast chrome and the board renderer\nreplay-viewer/           the wasm module, the OffscreenCanvas worker shell\ndata/vault.mapspec.json  the one authored map\nscripts/art/             the committed art and map generators\ntools/                   the CI hooks and the fixture recorder\n```\n\nLantern is forked from [`Metta-AI/coworld-ctf`](https://github.com/Metta-AI/coworld-ctf)\n(paintbot) — its integer physics, its fog-of-war cone, its pushable props and\nits broadcast chrome — with the server-side LLM client and the JSON replay\nshape taken from [`Metta-AI/cogame-bullwhip`](https://github.com/Metta-AI/cogame-bullwhip).\n"
      },
      "pages": [
        {
          "id": "rules.md",
          "title": "Rules",
          "content": {
            "type": "text",
            "value": "# Lantern — rules\n\n## The floor\n\nA walled warehouse, 1235 × 659 px, origin top-left, +x right, +y down. One\nauthored map, `vault`: six square pillars, four three-sided alcoves with\none-crate-wide doorways, two long racks, two baffles, and the **seeker pen** —\na 120 × 110 px room at the bottom-centre edge whose north door is solid during\na build act and open during a hunt. The obstacle set is invariant under a 180°\nrotation of the floor, so neither half plays a friendlier map than the other.\n\nStatic geometry is **not secret**. The blueprint is in every seat's\nobservation, both roles, always. What the dark hides is *where the crates\nended up and where the bodies are*.\n\n## Bodies\n\n- 12 × 12 px footprint, sub-pixel integer motion (`MotionScale = 256`).\n- Hiders top out at 704 sub-pixels/tick (≈ 66 px/s); seekers at 768 (+9 %),\n  because they have ground to cover.\n- **Crawling** caps a cog at 40 % of its top speed, makes no footstep sound,\n  and cannot push a crate.\n- Every cog carries an aim in **brads** (256 per turn, 0 = east,\n  counter-clockwise), turned at most 5 brads per tick.\n\n## Crates\n\nTen crates `C0`…`C9`, 48 × 48 px, solid and opaque. Three states:\n\n| state | pushable | breaks light | notes |\n|---|---|---|---|\n| `loose` | yes | yes | a hider shoves 6 px a tick, a seeker 4 |\n| `locked` | **no** | yes | bolted: only a pry gets through it |\n| `broken` | — | no | gone from the world |\n\nA **lock** takes 24 ticks (1 s) held still within 20 px of a loose crate, and\neach hider gets **three locks per half**. A **pry** takes 72 ticks (3 s) held\nstill within 20 px of a locked crate and ends in a 900 px sound ring — loud\nenough that everyone still hiding hears it. Any progress resets the moment the\ncog moves, releases the button, or changes target.\n\n## Light\n\nA seeker's lit set is everything within **420 px** whose bearing is within\n**±18 brads** (a 50.6° beam) of its aim **and** has line of sight through walls\nand live crates, plus an omni **60 px** bubble with line of sight. The three\nseekers **share one radio**: anything one of them lights is in all three\nseekers' observations that turn. Lanterns are OFF for every build tick.\n\nA hider is **FOUND** when it is held in the team's light for 12 consecutive\nticks (half a second) or when any seeker's body centre comes within 24 px of it\nwith line of sight. On a find it is teleported to the caught pen and is inert\nfor the rest of the half.\n\n## Sound\n\n| kind | radius | rate | jitter | who hears it |\n|---|---|---|---|---|\n| footstep | 260 px | once per cog per 24 ticks, only above half speed and not crawling | ±40 px | seekers |\n| crate push | 420 px | once per crate per 12 ticks | ±60 px | seekers |\n| crate break | 900 px | on the break | ±30 px | everyone |\n\nRings live 24 ticks. The jitter is deterministic (one PCG32 stream from the\nepisode seed) and gives a listener a *place*, not a point.\n\n## Heartbeat\n\nEvery hunt tick each seeker is told a five-band proximity reading off the\nstraight-line distance to the nearest unfound hider, with no direction:\n\n`burning ≤ 120 px`, `hot ≤ 260`, `warm ≤ 450`, `cool ≤ 750`, `cold > 750`.\n\nIt is real information, not decoration, and it is exactly what the viewer's\nheartbeat bars show.\n\n## The clock\n\n`dt = 1/24 s`. A **decision turn** is 120 ticks (5 s). At the first tick of a\nturn the server freezes the state, builds each active seat's view, collects one\norder per seat, and hands them to the control layer, which drives the cogs for\nall 120 ticks. During a build act only the three hiding seats are queried — the\nseekers are frozen in the pen and are not asked for an order they could not act\non.\n\n## Resolution order (every tick, no exceptions)\n\n1. Phase clock: derive half/act/turn; run the half reset at the boundary;\n   append a keyframe every 24 ticks.\n2. Frozen seats: during a build act a seeker's control is forced to zero, its\n   velocity is held at 0, its aim is held and its lantern is off.\n3. Control compile: the control layer reads the world and the seat's order.\n4. Quantise to `(move_x i8, move_y i8, aim_turn i8, action u8)`. **The sim\n   consumes only these bytes and the replay records only these bytes.**\n5. Aim.\n6. Motion: per-axis accelerate/clamp, friction on an axis with no input, wall\n   slide, then cog–cog separation with restitution.\n7. Crates: push along the dominant axis of the pusher's displacement, or revert\n   the pusher along that axis if the crate cannot move.\n8. Lock and pry.\n9. Occlusion rebake, if any crate moved, locked or broke.\n10. Lanterns and sight.\n11. Detection.\n12. Score accrual (hunt act only).\n13. Heartbeat bands and sound decay.\n14. Keyframe.\n15. Act / half / match end.\n\n## Half reset (exact, at the boundary)\n\nEvery crate returns to its authored start position in state `loose`; broken\ncrates return; every cog's velocity, aim and lock/pry progress are zeroed;\n`locks_used` is cleared; found hiders leave the caught pen; the new hiders\nspawn on the north line and the new seekers in the pen. **Nothing carries\nacross the intermission except the score.**\n\n## Scoring\n\n```\nhuntTicksPlayed(half)  hunt ticks actually simulated in that half\nhidden_ticks(seat)     hunt ticks in that seat's HIDING half while unfound\nf(team)                sum(hidden_ticks over the team's 3 seats)\n                       ------------------------------------------\n                            3 * huntTicksPlayed(its hiding half)\n\nscore(Moth) = 0.5 + 0.5 * (f(Moth) - f(Owl))\nscore(Owl)  = 1 - score(Moth)\n```\n\nHigher is better. Every seat carries its team's score, so `results.scores` sums\nto 3.0 across six seats. `f(Moth) == f(Owl)` is a draw: `winner: null`, every\n`win` false.\n\n## End conditions\n\n`results.reason` is a closed enum of exactly three values; `results.end_rule`\ncarries the detail.\n\n| `reason` | `end_rule` | when |\n|---|---|---|\n| `complete` | `full_time` | all ticks simulated. The normal ending. |\n| `deadline` | `wall_clock` | the wall-clock budget elapsed first. The score uses the hunt ticks actually played; if half 2's hunt never started, everybody scores 0.5. |\n| `fault` | `sim_fault` | a sim invariant tripped (a cog outside the arena, a crate in a wall, a negative hidden-tick count). All scores 0.5. |\n| `fault` | `host_error` | an unexpected server-side exception. Same treatment. |\n\nA seat that never connects does **not** end the episode: its cog plays the\n`warden` baseline for the whole match, the no-show is reported once to\n`COGAME_PLAYER_FAILURE_URI`, and the match plays to full time.\n\n## Determinism\n\nSame seed + same control bytes ⇒ the same state digest at every keyframe, in\nthe native build **and** in the emscripten viewer build. The whole step is\ninteger: no `sin`, no `cos`, no `atan2`, no float arithmetic anywhere on the\nstep path, and no `-ffast-math`. `tests/test_vision.nim` greps the source to\nkeep it that way, and `tests/test_determinism.nim` pins the digests to a\ncommitted golden fixture.\n"
          }
        },
        {
          "id": "protocol.md",
          "title": "Wire protocol",
          "content": {
            "type": "text",
            "value": "# Lantern — wire protocol\n\nThree surfaces: the **player** websocket (what a policy container speaks), the\n**global** spectator stream, and the **replay** bytes. All JSON, all UTF-8.\n\n## Player protocol — `lantern.player.v1`\n\nJSON text frames over the websocket named by `COWORLD_PLAYER_WS_URL` (the\nplatform already appends `?slot=N&token=T`). A bad slot or token gets a **403**\nand a second connection on a live slot gets a **409**.\n\nA lantern policy is a prompt, and decisions are made in the GAME server, so the\nplayer container has exactly one job. On connect it sends **one** frame:\n\n```json\n{\"type\": \"register\",\n \"prompt\": \"<strategy text, or empty>\",\n \"scripted\": \"warden\" | \"moth\" | null,\n \"policy\": \"<free label, <= 48 runes>\"}\n```\n\n`PLAYER_SCRIPTED` parsing: `warden` / `1` / `true` / `yes` → the warden\nbaseline, `moth` → the moth baseline, anything else → none. A seat that\nregisters with neither field, or never registers at all, plays `warden`.\nAn over-long `prompt` is truncated at 4000 runes, never rejected.\n\nThe server answers `welcome`, then one `turn` frame per decision turn, then the\nfinal frame:\n\n```json\n{\"type\": \"welcome\", \"protocol\": \"lantern.player.v1\", \"slot\": 0,\n \"alias\": \"Moth-1\", \"team\": \"Moth\", \"hides_in_half\": 1, \"turns\": 42}\n\n{\"type\": \"turn\", \"turn\": 17, \"tick\": 2040, \"half\": 1, \"act\": \"hunt\",\n \"role\": \"hider\", \"view\": { … }, \"order_source\": \"llm\"}\n\n{\"done\": true, \"result\": { …the results document… }}\n```\n\nThe `turn` frame is **informational**: the seat is not required to answer, and\nnothing it sends after `register` is read. It exists so a policy author can see\nexactly what its prompt was shown.\n\n## The per-seat view\n\nCoordinates are integers in map pixels. Two shapes, one per role.\n\n**Hider** — sees the full static map, **all ten crate positions and states**\n(it knows the fort it built: that asymmetry is what makes construction pay),\nits own trio, its clock and locks left, seekers within 700 px with line of\nsight, the bearing and band of any beam sweeping within 220 px, and break-ring\nsounds.\n\n```json\n{\"turn\": 4, \"of\": 42, \"half\": 1, \"act\": \"build\",\n \"clock\": {\"act_left_s\": 10.0, \"hunt_left_s\": 75.0},\n \"you\": {\"alias\": \"Moth-2\", \"pos\": [612, 118], \"aim\": 64, \"crawl\": false,\n         \"found\": false, \"hidden_s\": 0.0, \"locks_left\": 3},\n \"map\": {\"w\": 1235, \"h\": 659, \"walls\": [[0,0,1235,16], …],\n         \"nooks\": [{\"anchor\": [240,329], \"opening\": [[196,306],[196,353]]}, …],\n         \"pen\": [558, 545, 120, 110]},\n \"crates\": [{\"id\": \"C0\", \"pos\": [150,329], \"state\": \"loose\"}, … 10 … ],\n \"team\": [{\"alias\": \"Moth-1\", \"pos\": [150,110], \"found\": false, \"hidden_s\": 0.0}, …],\n \"seekers_seen\": [{\"alias\": \"Owl-3\", \"pos\": [700,540], \"aim\": 96, \"dist\": 430}],\n \"beams\": [{\"bearing\": 192, \"band\": \"near\"}],\n \"sounds\": [{\"kind\": \"break\", \"pos\": [742,520], \"age_ticks\": 8}],\n \"found_count\": 0,\n \"your_last_order\": { … or null on turn 0 … }}\n```\n\n**Seeker** — sees the full static map, its own position/aim/heartbeat, **only\nwhat the team's three lanterns currently light**, its team-mates' positions and\nbands, every sound ring within earshot (jittered), the found list and the\nclock. It does **not** see unlit crates, unlit hiders, the hiders' orders,\nnotes or `say` text, the exact heartbeat distance, or the seed.\n\n```json\n{\"turn\": 19, \"of\": 42, \"half\": 1, \"act\": \"hunt\",\n \"clock\": {\"act_left_s\": 40.0},\n \"you\": {\"alias\": \"Owl-1\", \"pos\": [430,300], \"aim\": 32,\n         \"heartbeat\": \"warm\", \"prying\": null},\n \"map\": { …identical to the hider's map block… },\n \"lit\": {\"crates\": [{\"id\": \"C4\", \"pos\": [450,380], \"state\": \"locked\"}],\n         \"hiders\": [{\"alias\": \"Moth-3\", \"pos\": [910,215], \"lit_by\": \"Owl-2\",\n                     \"streak_ticks\": 4}]},\n \"team\": [{\"alias\": \"Owl-2\", \"pos\": [900,240], \"aim\": 200, \"heartbeat\": \"hot\"}, …],\n \"sounds\": [{\"kind\": \"push\", \"pos\": [560,180], \"age_ticks\": 14}],\n \"found\": [{\"alias\": \"Moth-1\", \"at_s\": 21.5, \"by\": \"Owl-2\", \"mode\": \"beam\"}],\n \"found_count\": 1, \"hiders_left\": 2,\n \"your_last_order\": { … }}\n```\n\nIn `found[]`, `by` is the seeker that made the find and `mode` is `beam` or\n`tag`, exactly as in the `found` replay event; `at_s` is seconds since this\nhalf's hunt act began, the clock the seat itself is on.\n\nHidden from **everyone**, both roles: the opponent's prompts, notes and `say`\nstrings (they exist only in the replay, for spectators), the real player names\nbehind the aliases, and future ticks.\n\n## The order\n\nOne per seat per turn. The scripted baselines emit the identical shape, so the\ntwo policy kinds are strictly comparable.\n\n```json\n{\"intent\": \"push\", \"target\": [196, 329], \"crate\": \"C4\",\n \"aim\": \"target\", \"crawl\": false,\n \"note\": \"screening the west alcove before the lights go\", \"say\": \"west screen\"}\n```\n\n| field | legal values | repair when violated |\n|---|---|---|\n| `intent` | hider: `push` `lock` `hide` `flee` `scout` `wait`; seeker: `sweep` `beeline` `chase` `pry` `hold` `wait` | legal-for-the-other-role → `hide` / `sweep`; unknown → a retry |\n| `target` | `[int, int]`, clamped to x ∈ [8, 1227], y ∈ [8, 651] | missing → the cog's current position |\n| `crate` | `\"C0\"`…`\"C9\"`, case-insensitive, ≤ 4 runes; an integer 0–9 also accepted | unknown → the nearest crate legal for the intent; none legal → the intent degrades |\n| `aim` | `sweep` `hold` `track` `target` | → `target`; ignored for hiders |\n| `crawl` | `true`/`false`, also `\"true\"`/`\"false\"`/`0`/`1` | → `false`; forced `false` for a seeker |\n| `note` | ≤ 140 **runes** | truncated on a rune boundary |\n| `say` | ≤ 32 **runes** | truncated on a rune boundary |\n\nThree further caps on strings that reach the replay: `register.policy` ≤ 48\nrunes, `fallback.detail` ≤ 200 runes, `register.prompt` ≤ 4000 runes at the\ntransport. **Truncation is on rune boundaries, never bytes** — a byte-truncated\nmulti-byte character renders in a browser and then fails a strict JSON parser.\n\nParsing is tolerant: markdown fences are stripped, the outermost balanced\n`{…}` is taken if the model prefixed prose, numeric strings are accepted for\n`target`, and `crate` may be an integer. Only when no object with a usable\n`intent` can be recovered does the retry fire, and only when the retry also\nfails does the seat get the `warden` order plus a `fallback` event.\n\n## Decisions and timing\n\nAll open seats' requests go out as **ONE PARALLEL BATCH** per turn\n(`curly.makeRequests`) — never a sequential walk. A hunt turn batches six\nrequests, a build turn three. Per turn, per seat: attempt 1 gets 8.5 s, the one\nretry gets 3.5 s with a \"your previous reply was invalid\" hint, and then the\nscripted order. At the start of each turn, if two more full turn budgets would\nnot fit inside `wallClockBudgetSeconds`, the **budget guard** engages and every\nremaining turn plays scripted, so the episode ends `complete/full_time` rather\nthan `deadline`.\n\n## Spectator stream — `/global`\n\nA whole-world snapshot on connect and every few seconds after: tick, turn,\nhalf, act, the map block, every cog (alias, team, role, position, aim, crawl,\nfound, hidden ticks, heartbeat, current intent/note/say/source), every crate,\nlive sound rings, the policy names and the team colours. Nothing is hidden from\na spectator — that is the point of the second name space.\n\n## Replay bytes — `lantern.replay.v1`\n\nStrict UTF-8 JSON, self-sufficient: the viewer contacts nothing but the S3 URL\nit was given.\n\n```json\n{\"protocol\": \"lantern.replay.v1\", \"format_version\": 1, \"game_version\": \"1\",\n \"seed\": 679961,\n \"config\": { …the resolved config, tokens excluded… },\n \"map\": { …data/vault.mapspec.json inlined verbatim… },\n \"names\": {\"players\": […], \"aliases\": […], \"teams\": […],\n           \"policy_kinds\": […], \"colors\": {\"Moth\": \"#f2c14e\", \"Owl\": \"#4ecdc4\"}},\n \"ticks_per_second\": 24, \"turn_ticks\": 120, \"tick_count\": 5040,\n \"phases\": [{\"half\": 1, \"act\": \"build\", \"from\": 0, \"to\": 719}, …],\n \"controls_b64\": \"<base64 of tick_count x 6 x 4 bytes>\",\n \"keyframes\": [{\"t\": 0, \"d\": 2947483111, \"cogs\": [[150,110,64,0], …],\n                \"crates\": [[150,329,0], …], \"hb\": [0,0,0], \"hid\": [0,0,0,0,0,0]}, …],\n \"events\": [ … ],\n \"results\": { … }}\n```\n\nCog state codes: `0` active, `1` frozen in the pen, `2` crawling, `3` found.\nCrate state codes: `0` loose, `1` locked, `2` broken. `hb` is the three\nseekers' heartbeat bands (0 cold … 4 burning); `hid` the six hidden-tick\ncounters.\n\n### Event vocabulary\n\nEvery record carries `t` (tick), plus `turn`, `half`, `act` where meaningful.\n\n| `type` | fields |\n|---|---|\n| `match_start` | `seed`, `map`, `aliases`, `teams`, `hid_in_half` |\n| `half_start` | `half`, `hiders`, `seekers` |\n| `act_start` | `half`, `act` |\n| `turn_start` | `turn`, `half`, `act`, `hidden_s` (per team), `hiders_left` |\n| `order` | `turn`, `seat`, `alias`, `role`, `source` (`llm`/`scripted`/`fallback`), `latency_ms`, `intent`, `target`, `crate`, `aim`, `crawl`, `note`, `say` |\n| `fallback` | `turn`, `seat`, `attempt`, `cause`, `detail` |\n| `budget_guard` | `turn`, `remaining_s` |\n| `crate_push` | `seat`, `alias`, `crate`, `from`, `to` |\n| `crate_lock` | `seat`, `alias`, `crate`, `pos` |\n| `crate_pry` | `seat`, `alias`, `crate`, `pct` |\n| `crate_break` | `seat`, `alias`, `crate`, `pos` |\n| `sound` | `kind`, `pos` (jittered), `radius` |\n| `spot` | `seeker`, `hider`, `dist` |\n| `found` | `half`, `hider`, `seeker`, `mode` (`beam`/`tag`), `hidden_s`, `hiders_left` |\n| `act_end` | `half`, `act`, `reason` (`time`/`all_found`) |\n| `half_end` | `half`, `hidden_frac`, `hidden_s`, `per_hider` |\n| `end` | `reason`, `end_rule`, `scores`, `team_hidden_frac`, `winner` |\n\n## Results document\n\nPer-seat arrays are length 6 in slot order; `team_*` arrays are length 2, index\n0 = Moth, 1 = Owl. The key set is closed and must equal the manifest's\n`results_schema` — `tests/test_manifest.nim` compares the two.\n\n```json\n{\"names\": [...], \"aliases\": [...], \"teams\": [...], \"hid_in_half\": [...],\n \"policy_kinds\": [...], \"scores\": [...], \"win\": [...],\n \"hidden_ticks\": [...], \"hidden_seconds\": [...], \"finds\": [...],\n \"crates_pushed\": [...], \"crates_locked\": [...], \"crates_broken\": [...],\n \"team_hidden_frac\": [0.569, 0.813], \"team_hidden_seconds\": [128.0, 183.0],\n \"reason\": \"complete\", \"end_rule\": \"full_time\", \"winner\": 1,\n \"final_tick\": 5040, \"final_turn\": 42, \"halves_played\": 2,\n \"hunt_ticks_played\": [1800, 1800], \"seed\": 679961,\n \"llm_turns\": [...], \"fallback_turns\": [...], \"fallback_causes\": [...]}\n```\n\n## Runtime contract\n\n`COGAME_CONFIG_URI` (required), `COGAME_RESULTS_URI`, `COGAME_SAVE_REPLAY_URI`,\n`COGAME_LOAD_REPLAY_URI`, `COGAME_PLAYER_FAILURE_URI`, `COGAME_EVENTS_URI` and\n`COGAME_METRICS_URI` (**file:// only** — any other scheme is refused loudly at\nstartup), `COGAME_HOST`, `COGAME_PORT`. At the end of an episode the server\nbroadcasts `done` to every seat with a bounded 3 s wait, writes the replay, and\nthen writes the results — in that order, because the hosted worker tears the\nplayer pods down the moment `results.json` exists.\n"
          }
        }
      ]
    }
  },
  "player": [
    {
      "id": "baseline",
      "name": "warden",
      "type": "player",
      "description": "The bundled certification player: registers as the warden scripted baseline and plays deterministically, with no LLM. Field your own policy by uploading this same image with PLAYER_PROMPT set to your strategy.",
      "image": "{{PLAYER_IMAGE}}",
      "run": [
        "/bin/lantern-player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "warden"
      },
      "resources": {
        "requests": {
          "cpu": "100m",
          "memory": "64Mi"
        },
        "limits": {
          "cpu": "1"
        }
      },
      "source_url": "https://github.com/Metta-AI/cogame-lantern/tree/main"
    }
  ],
  "variants": [
    {
      "id": "default",
      "name": "Warehouse (2 teams x 3, two halves)",
      "description": "The full match: a 30 s build act and a 75 s hunt in each half, 5040 ticks, 42 decision turns.",
      "game_config": {
        "players": [
          {
            "name": "P1"
          },
          {
            "name": "P2"
          },
          {
            "name": "P3"
          },
          {
            "name": "P4"
          },
          {
            "name": "P5"
          },
          {
            "name": "P6"
          }
        ],
        "slots": [
          {
            "team": "moth"
          },
          {
            "team": "owl"
          },
          {
            "team": "moth"
          },
          {
            "team": "owl"
          },
          {
            "team": "moth"
          },
          {
            "team": "owl"
          }
        ],
        "num_agents": 6,
        "prepTicks": 720,
        "huntTicks": 1800,
        "turnTicks": 120,
        "halves": 2,
        "turnBudgetSeconds": 13,
        "wallClockBudgetSeconds": 660,
        "playerConnectTimeoutSeconds": 90,
        "mapPath": "vault"
      }
    },
    {
      "id": "sprint",
      "name": "Sprint (2 teams x 3, short halves)",
      "description": "Cheap ladder rounds: a 20 s build act and a 45 s hunt in each half, 3120 ticks, 26 decision turns. Same six seats, same map, same rules - only the act lengths change.",
      "game_config": {
        "players": [
          {
            "name": "P1"
          },
          {
            "name": "P2"
          },
          {
            "name": "P3"
          },
          {
            "name": "P4"
          },
          {
            "name": "P5"
          },
          {
            "name": "P6"
          }
        ],
        "slots": [
          {
            "team": "moth"
          },
          {
            "team": "owl"
          },
          {
            "team": "moth"
          },
          {
            "team": "owl"
          },
          {
            "team": "moth"
          },
          {
            "team": "owl"
          }
        ],
        "num_agents": 6,
        "prepTicks": 480,
        "huntTicks": 1080,
        "turnTicks": 120,
        "halves": 2,
        "turnBudgetSeconds": 13,
        "wallClockBudgetSeconds": 400,
        "playerConnectTimeoutSeconds": 90,
        "mapPath": "vault"
      }
    }
  ],
  "certification": {
    "game_config": {
      "players": [
        {
          "name": "P1"
        },
        {
          "name": "P2"
        },
        {
          "name": "P3"
        },
        {
          "name": "P4"
        },
        {
          "name": "P5"
        },
        {
          "name": "P6"
        }
      ],
      "slots": [
        {
          "team": "moth"
        },
        {
          "team": "owl"
        },
        {
          "team": "moth"
        },
        {
          "team": "owl"
        },
        {
          "team": "moth"
        },
        {
          "team": "owl"
        }
      ],
      "num_agents": 6,
      "seed": 42,
      "prepTicks": 240,
      "huntTicks": 480,
      "turnTicks": 120,
      "halves": 2,
      "turnBudgetSeconds": 13,
      "wallClockBudgetSeconds": 180,
      "playerConnectTimeoutSeconds": 60,
      "mapPath": "vault"
    },
    "players": [
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      }
    ]
  }
}
